# ================================================================
# Citlivé — NIKDY necommitovat
# ================================================================
.credentials.json
*.token
*.pem
*.key
id_rsa*

# ================================================================
# Session data a historie (obsahuje prompty, výstupy, kontext)
# ================================================================
history.jsonl
sessions/
projects/
file-history/
paste-cache/
shell-snapshots/
backups/
telemetry/
downloads/
tasks/
/plans/
cache/

# ================================================================
# Lokální stav / usage tracking
# ================================================================
levis-usage.json
*.log
*-usage.json
.last-update-result.json
.claude/
session-env/

# ================================================================
# Runtime cache (MCP auth, per-machine state)
# ================================================================
mcp-*-cache.json

# ================================================================
# Claude Code v2.1+ daemon / jobs (per-machine supervisor state)
# ================================================================
daemon.lock
daemon.status.json
daemon.log
daemon.json
daemon/
jobs/

# ================================================================
# Per-machine backupy (necommitovat — obsahují stroj-specifické cesty)
# ================================================================
settings.*-backup.json
settings.local.json

# ================================================================
# Plugins & externí skills (instalovat zvlášť)
# ================================================================
plugins/
skills/trailofbits/

# ================================================================
# Visual-audit runner — node_modules, install lokálně přes npm i
# (runner/ byl přejmenován na scripts/, zbytek na disku je mrtvý)
# ================================================================
skills/visual-audit/runner/
skills/visual-audit/scripts/node_modules/
skills/visual-audit/scripts/.audit/
.last-cleanup

# ================================================================
# Runtime telemetrie — všechny JSONL logy v logs/ (token-aware, time-aware, …)
# ================================================================
logs/*.jsonl

# ================================================================
# Systémové
# ================================================================
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Runtime state daemonu a usage trackeru
daemon-auth-cooldown
daemon-auth-status.json
levis-usage-history.json

# Sandbox a stav doc-check hooku
cache/doc-check-state.json

# ================================================================
# Server-pushed policy / remote config (runtime, per-machine)
# ================================================================
policy-limits.json
remote-settings.json
